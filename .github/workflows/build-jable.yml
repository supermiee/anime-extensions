name: Build Extensions

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build APKs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      - name: Build Jable
        run: |
          chmod +x gradlew
          ./gradlew :src:all:jable:assembleRelease --no-daemon

      - name: Build MissAV
        run: ./gradlew :src:all:missav:assembleRelease --no-daemon

      - name: Find APKs
        id: apk
        run: |
          JABLE=$(find . -name "*.apk" -path "*/jable/*" | head -1)
          MISSAV=$(find . -name "*.apk" -path "*/missav/*" | head -1)
          echo "jable=$JABLE" >> $GITHUB_OUTPUT
          echo "missav=$MISSAV" >> $GITHUB_OUTPUT
          echo "Jable: $JABLE"
          echo "MissAV: $MISSAV"

      - name: Release
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Extensions (Auto Build)
          body: |
            ## Jable Extension
            - Uses WebView to bypass Cloudflare TLS fingerprinting

            ## MissAV Extension
            - Uses WebView to bypass Cloudflare for video CDN

            Commit: ${{ github.sha }}
          files: |
            ${{ steps.apk.outputs.jable }}
            ${{ steps.apk.outputs.missav }}
