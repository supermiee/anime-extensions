name: Build MissAV

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build MissAV
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin

      - name: Build MissAV
        run: |
          chmod +x gradlew
          ./gradlew :src:all:missav:assembleRelease --no-daemon

      - name: Find APK
        id: apk
        run: |
          find . -name "*.apk" -type f
          MISSAV=$(find . -name "*.apk" -type f | head -1)
          echo "missav=$MISSAV" >> $GITHUB_OUTPUT
          echo "Found: $MISSAV"

      - name: Release
        if: success() && steps.apk.outputs.missav != ''
        uses: softprops/action-gh-release@v2
        with:
          tag_name: missav-latest
          name: MissAV Extension
          files: ${{ steps.apk.outputs.missav }}
